{
  "name": "readme-generator",
  "version": "1.0.0",
  "description": "generates professional readme from command line inputs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "lane pemberton",
  "license": "ISC",
  "dependencies": {
    "inquirer": "^8.0.0"
  }
}
